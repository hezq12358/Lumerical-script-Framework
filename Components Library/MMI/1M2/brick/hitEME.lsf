#  提取数据

if (wave_opt == 0)
	{
Se = getemesweep("S");	# 获取长度扫描信息
	}
else 
	{
Se = getemesweep("S_wavelength_sweep"); # 获取波长扫描信息
	}

# 散射系数
sc21 = Se.s21;     # 下路  
sc31 = Se.s31;     # 上路
# 功率传输
po21 = abs(sc21)^2;
po31 = abs(sc31)^2;
# 相位
ph21 = angle(sc21);
ph31 = angle(sc31);


# 循环时，请注释掉 
plot( lengthMMA*M, 10*log10(po21), 10*log10(po31) );